name: ZIA Video - YouTube 17:30

on:
  workflow_dispatch:   # disparado por clock.yml a las 17:30 España

jobs:
  publicar-video:
    runs-on: ubuntu-latest
    timeout-minutes: 35

    steps:
      - name: Descargar código
        uses: actions/checkout@v4

      - name: Instalar Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Instalar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Caché node_modules (Remotion)
        uses: actions/cache@v4
        with:
          path: remotion/node_modules
          key: npm-${{ runner.os }}-${{ hashFiles('remotion/package.json') }}
          restore-keys: npm-${{ runner.os }}-

      - name: Caché Chrome headless (Remotion)
        uses: actions/cache@v4
        with:
          path: ~/.cache/remotion
          key: remotion-chrome-${{ runner.os }}-v1

      - name: Instalar dependencias del sistema
        run: sudo apt-get update && sudo apt-get install -y fonts-dejavu fonts-noto-core ffmpeg

      - name: Instalar dependencias Python
        run: pip install -r requirements.txt

      - name: Instalar dependencias Remotion
        run: npm install
        working-directory: remotion

      - name: Generar y publicar video
        env:
          YOUTUBE_CLIENT_ID: ${{ secrets.YOUTUBE_CLIENT_ID }}
          YOUTUBE_CLIENT_SECRET: ${{ secrets.YOUTUBE_CLIENT_SECRET }}
          YOUTUBE_REFRESH_TOKEN: ${{ secrets.YOUTUBE_REFRESH_TOKEN }}
          PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
        run: python main.py youtube 2>&1 | tee debug.txt; exit ${PIPESTATUS[0]}

      - name: Mostrar log si falla
        if: failure()
        run: cat debug.txt || echo "(sin log)"
